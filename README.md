# SC_route_generator
Automatically computes and outputs total travel distances between two selected Star Citizen locations in space, rounded to the nearest 0.01 Gigameter. Useful for route planning, even through jump points.
